import { ReminderDeleteDirective } from './reminder-delete.directive';

describe('ReminderDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new ReminderDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
